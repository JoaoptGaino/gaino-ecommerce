import { GainoShopValidators } from './gaino-shop-validators';

describe('GainoShopValidators', () => {
  it('should create an instance', () => {
    expect(new GainoShopValidators()).toBeTruthy();
  });
});
